module.exports = {
    extends: [
        'stylelint-config-standard',
        <% if (cssProcessor === 'sass') { %>'stylelint-config-recommended-scss',<% } %>
    ].filter(Boolean),
    <% if (cssProcessor === 'sass') { %>
    plugins: ['stylelint-scss'],
    <% } %>
    syntax: <% if (cssProcessor === 'less') { %>'less'<% } else if (cssProcessor === 'stylus') { %>'stylus'<% } else if (cssProcessor === 'sass') { %>'scss'<% } else { %>'css'<% } %>,
    rules: {
        'color-no-invalid-hex': true,
        'block-no-empty': true,
        'unit-no-unknown': true,
        'declaration-block-trailing-semicolon': 'always',
        'selector-pseudo-class-no-unknown': [true, { ignorePseudoClasses: ['global'] }],
        <% if (cssProcessor === 'sass') { %>
        'scss/dollar-variable-colon-space-after': 'always',
        'scss/percent-placeholder-pattern': '^foo',
        <% } %>
        <% if (cssProcessor === 'less') { %>
        <% } %>
        <% if (cssProcessor === 'stylus') { %>
        <% } %>
    },
    <% if (cssProcessor === 'sass') { %>
    ignoreFiles: ['**/node_modules/**'],
    <% } %>
};
